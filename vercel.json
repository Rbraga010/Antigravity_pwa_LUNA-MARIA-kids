{
  "buildCommand": "cd frontend && npm install && npx prisma generate --schema=../prisma/schema.prisma && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && npm install --prefix frontend"
}
